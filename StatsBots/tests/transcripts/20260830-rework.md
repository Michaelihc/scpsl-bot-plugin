# StatsBots rework verification — 2026-08-30

Environment:

- Windows / PowerShell
- .NET SDK build of `net48` plugin and pure console tests
- `SL_REFERENCES=C:\Program Files (x86)\Steam\steamapps\common\SCP Secret Laboratory Dedicated Server\SCPSL_Data\Managed`

Automated results:

- `dotnet build StatsBots\StatsBots.csproj -c Release`: passed, 0 warnings, 0 errors.
- `dotnet run --project StatsBots\tests\StatsBots.Tests.csproj -c Release`: 7/7 passed.
- Pure coverage: authenticated identity and `ID_Dummy`; exact scoring matrix and clamp; bounded duplicate callback suppression under a 600-event burst; tier/title validation and removed catalog entries; 59:59/60:00/60:01 playtime boundary and notice cadence; deterministic shuffled tips with no immediate repeat; provider missing/throw/loading/verified readiness, asynchronous offline hydration, and persisted counters.
- Assembly/project metadata: semantic version 1.0.0, assembly/file version 1.0.0.0.
- Direct UI scan: no `Player.SendHint` call exists; HSM absence fails quiet without touching the shared native hint channel.
- Broadcast scan: one native per-player `SendBroadcast` call with `shouldClearPrevious:false`; no `ClearBroadcasts` call exists.
- Player identity scan: provider writes flow through full authenticated UserId normalization; no `ID_Dummy` write path.
- EN/CN HSM harness: all ten combinations across five native backgrounds pass static checks and `--fail-on-collision`; see `tests/ui/20260830-results.md`. Example local captures: `tests/ui/output/statsbots-en-spawn-flash-highlighted.png` and `tests/ui/output/statsbots-cn-inventory-highlighted.png`.

Manual multiplayer/integration checks still required:

- Load StatsSystem, SCPSLBot, HSM, and the ServerKeybinds compatibility assembly on the dedicated bot-test port 8888.
- Verify two-client scoring/death callback behavior and persistence through restart.
- Verify SSS acquisition is baseline-only and later deliberate title/toggle changes refresh only the affected client.
- Verify missing/late/failing StatsSystem and HSM states, asynchronous offline hydration retry, and native broadcast coexistence.
