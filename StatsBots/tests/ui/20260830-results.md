# StatsBots HSM harness results — 2026-08-30

Inputs:

- `statsbots-en.json`
- `statsbots-cn.json`

Both localized profiles passed the shared `..\.tests\UI` static checks at 1920x1080: center alignment only, safe caret spans, no wrapping warnings, no viewport overflow, and no overlap between the three StatsBots entries.

Native-background runs:

| Background | English | Chinese | Result |
|---|---:|---:|---|
| announcements/stat bar | pass | pass | no registered native collision |
| spectator | pass | pass | no registered native collision |
| waiting for players | pass | pass | no registered native collision; visually inspected |
| inventory | pass | pass | visible ink remains between the native inventory list and wheel |
| spawn flash | pass | pass | visible ink remains left of the native F1 prompt |

All ten language/background combinations exited 0 with `--fail-on-collision`. The profile uses HSM Center alignment, X `-800`, abbreviated visible text, and a transparent forty-five-cell 1em tail on every row. Its default Y values remain near the audited bands at 735/780/850. Generated PNGs stay in ignored `output/` and can be reproduced with `render-image.js`.
